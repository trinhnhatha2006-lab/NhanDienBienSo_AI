# Model YOLO

Đặt model phát hiện biển số `best.pt` vào thư mục này để dùng lệnh mặc định:

```powershell
python main.py ".\BienSoXe\xe_01.jpg"
```

Hoặc giữ model ở vị trí khác và truyền `--model "đường-dẫn\best.pt"`.

File `.pt` không được đưa vào Git vì có dung lượng lớn.
