services:
  recognition:
    build:
      context: .
    image: nhan-dien-bien-so:local
    volumes:
      - ./BienSoXe:/app/media:ro
      - ./runs:/app/runs
      - easyocr-models:/root/.EasyOCR

volumes:
  easyocr-models: